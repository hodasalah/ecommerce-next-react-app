import { createSlice } from "@reduxjs/toolkit";
export const cartSlice = createSlice({
  name: "cart",
  initialState: [],
  reducers: {},
})
// export cost {}=cartSlice.actions
export default cartSlice.reducer